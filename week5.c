#include<stdio.h>
#include<math.h>

int main(){
int a; // กำหนดให้ a เป็นจำนวนเต็ม int
for(a=0;a<=20;a++) // กำหนด a =0 ; และค่าตัวแปร a จะเพิ่มขึ้นทุกๆ 1 แต่ตัวแปร a ต้องน้อยกว่า 5
{
  printf("%d ", a*5);
 // เมื่อตัวแปร a น้อยกว่า 5 ให้พิมพ์ ###
}
return 0; // // เมื่อตัวแปร a ไม่น้อยกว่า 5 ให้พิมพ์ end for
}

