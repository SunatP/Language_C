#include<stdio.h>
#include<math.h>

int main()
{
    int arr[5],i;
        scanf("%d %d %d %d %d",&arr[0],&arr[1],&arr[2],&arr[3],&arr[4]);
    for(i=0;i<arr[i];i++)
    {
        arr[i] = arr[i]*arr[i];
    }

        printf("%d %d %d %d %d",arr[0],arr[1],arr[2],arr[3],arr[4]);
        return arr[i];
}
